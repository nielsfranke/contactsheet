# ContactSheet ML sidecar — CPU-only ONNX inference. No PyTorch (that's the whole point: it keeps
# the image small enough to ship as an optional add-on).
fastapi==0.139.0
uvicorn[standard]==0.32.1
onnxruntime==1.20.1
huggingface_hub==0.36.2
Pillow==12.3.0
numpy==2.2.1
# No `transformers`: 5.x image processors hard-require PyTorch/Torchvision (which would defeat the
# purpose of this container) and the 4.x line ended at 4.57.6, so its advisories are unfixable by
# pin. runtime.py does the two things we used itself — SigLIP image pre-processing in NumPy, and
# tokenisation straight from the repo's tokenizer.json via this Rust library (which is what
# transformers wrapped anyway). Verified bit-identical on both paths. `sentencepiece` went with it:
# the ONNX repos ship tokenizer.json, never a spiece.model.
tokenizers==0.23.2
