[pytest]
testpaths = tests
# --timeout: a hung test fails after 120 s instead of stalling the run.
# --strict-markers: a typo'd @pytest.mark.* is an error, not a silently-ignored marker.
addopts = -q --timeout=120 --strict-markers
filterwarnings =
    ignore::DeprecationWarning
