fastapi==0.138.0
# Pinned explicitly (normally a transitive dep of fastapi): the 0.41.x starlette that older
# fastapi pulled had several CVEs (DoS + multipart). 1.3.1 clears them; fastapi 0.138 supports it.
starlette==1.3.1
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.3
pydantic-settings==2.7.0
PyJWT==2.13.0
bcrypt==4.2.1
python-multipart==0.0.31
Pillow==12.2.0
# Camera-RAW preview extraction (rawpy.extract_thumb pulls the embedded JPEG — no full demosaic).
# Wheels bundle libraw, so no system packages are needed. NumPy (below) covers the bitmap-thumb path.
rawpy==0.24.0
slowapi==0.1.9
apprise==1.9.0
# Streaming ZIP downloads: generates the archive on the fly (no temp file) and, in sized mode with
# STORED, exposes the exact total length up front so the response can set a real Content-Length.
zipstream-ng==1.9.2
# Semantic search: NumPy powers the brute-force cosine ranking over stored embeddings; httpx is
# the client for the optional ML sidecar. The encoder model itself lives in the sidecar image, not
# here — the backend stays free of any ML runtime.
numpy==2.2.1
httpx==0.28.1
